import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vekstorm.main',
  appName: 'main-app',
  webDir: 'dist/main-app/browser'
};

export default config;
